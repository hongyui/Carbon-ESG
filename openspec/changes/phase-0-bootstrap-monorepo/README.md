# phase-0-bootstrap-monorepo

Phase 0 of the refactor: scaffold Laravel 12 backend, Next.js 16 frontend, docker-compose for local deps (mysql/redis/mailpit), and root tooling (.nvmrc, .env.example, .gitignore). Legacy code was preserved in commit f052705 — this change only covers the new stack scaffold.
