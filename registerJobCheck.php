<?php
  // require './pdo.php';
  // $id = isset($_GET['id']) ? $_GET['id'] : die('Missing id parameter');
  // $pdo = $database->getPdo();
  
  // $stmt = $pdo->prepare("UPDATE jobinfo SET isCheck = '是' WHERE id = :id");
  // $stmt->bindParam(':id', $id);
  // $stmt->execute();
  // header('Location: state.php');

?>